version: '3.8'

services:
  knowledge_api:
    build: .
    ports:
      - "8100:8100"
    volumes:
      - .:/app
    environment:
      - PORT=8100
      - NAME="Knowledge Database"

  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage:z
